services:
  redis:
    image: redis:7-alpine
    container_name: gongmozip-redis-local
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-local-data:/data

volumes:
  redis-local-data:
